{
  "name": "@aeswibon/pipeline-compose-action-export",
  "version": "1.17.1",
  "private": true,
  "type": "module",
  "description": "Upload pipeline-compose stage outputs artifact",
  "scripts": {
    "build": "tsc --noEmit",
    "bundle": "pnpm --filter @aeswibon/pipeline-compose-core run build && ncc build src/index.ts -o dist -s",
    "test": "vitest run --dir src"
  },
  "dependencies": {
    "@actions/artifact": "^6.2.1",
    "@actions/core": "^3.0.1",
    "@aeswibon/pipeline-compose-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@vercel/ncc": "^0.44.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
