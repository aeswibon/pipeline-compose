{
  "name": "@aeswibon/pipeline-compose-action-run",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "bundle": "pnpm --filter @aeswibon/pipeline-compose-core run build && ncc build src/index.ts -o dist -s",
    "test": "vitest run --dir src"
  },
  "dependencies": {
    "@actions/artifact": "^6.2.1",
    "@actions/core": "^3.0.1",
    "@aeswibon/pipeline-compose-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
