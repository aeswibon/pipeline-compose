version: 2
companion_workflows:
  - .github/workflows/release.yml
pipelines:
  cross-repo:
    stages:
      - id: remote-echo
        repo: your-org/your-target-repo
        workflow: .github/workflows/echo.yml
        outputs:
          - message
