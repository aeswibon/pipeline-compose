version: 2
pipelines:
  smoke-cross-repo:
    stages:
      - id: remote-echo
        repo: aeswibon/pipeline-compose-smoke-target
        workflow: .github/workflows/echo.yml
        outputs:
          - message
